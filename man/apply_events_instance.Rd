% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event-store.R
\name{apply_events_instance}
\alias{apply_events_instance}
\title{Overlay an instance's events onto an expanded instance table}
\usage{
apply_events_instance(
  instance_table,
  store = ".runs",
  instance = "instance",
  stamp = NULL
)
}
\arguments{
\item{instance_table}{An expanded instance table (see [expand_instance()]).}

\item{store}{Store directory.}

\item{instance}{Instance id to filter by, or `NULL` for all instances.}

\item{stamp}{Poll counter (ignored).}
}
\value{
`instance_table` with `assignee`, `due`, `status` folded from the store.
}
\description{
The multi-instance counterpart of [apply_events()]: rows of an
[expand_instance()] table are keyed by (task, element), and so are the events
folded onto them. Single rows (no `element`) read the same events
[apply_events()] reads, so the two views can never disagree.
}
\keyword{internal}
