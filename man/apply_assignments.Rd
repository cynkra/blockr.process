% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expr-builders.R
\name{apply_assignments}
\alias{apply_assignments}
\title{Apply instance assignments to a wide process table}
\usage{
apply_assignments(df, assignments = list())
}
\arguments{
\item{df}{A wide process table (one row per task).}

\item{assignments}{Named list: task id -> `list(assignee =, due =, status =)`.
Missing fields leave the table value untouched.}
}
\value{
The wide table with instance columns applied.
}
\description{
The instance block's workhorse, exported because the block's expression calls
it at evaluation time. Ensures the instance columns (`assignee`, `due`, `status`)
exist and overrides them per task from `assignments`.
}
