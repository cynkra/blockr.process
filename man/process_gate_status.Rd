% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process-table.R
\name{process_gate_status}
\alias{process_gate_status}
\title{Combine per-gate verdicts into a display status}
\usage{
process_gate_status(met, pending, need)
}
\arguments{
\item{met, pending}{How many gates are satisfied, how many may still be.}

\item{need}{The quorum ([process_quorum()]).}
}
\value{
`"open"`, `"blocked"` or `"skipped"`.
}
\description{
The one rule both [process_status()] and its multi-instance counterpart
in blockr.process apply, so that `all` and a quorum differ in a number
and nowhere else: reach the quorum and the task is ready; fall short of
it even if everything still open went your way and it can never be
reached, so the task is skipped; otherwise wait.
}
\keyword{internal}
