% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/worker.R
\name{rearm_loops}
\alias{rearm_loops}
\title{Re-arm the checks a just-finished task loops back into}
\usage{
rearm_loops(
  df,
  finished,
  store = ".runs",
  instance = "instance",
  element = NULL
)
}
\arguments{
\item{df}{The table with the instance applied.}

\item{finished}{Task that just reached a terminal status.}

\item{store}{Store directory.}

\item{instance}{Instance id.}

\item{element}{Element the task belongs to, or `NULL` for single tasks.
Loops inside a multi-instance group are re-armed per element: sending
one region back must not re-open the checks of the others.}
}
\value{
Character vector of re-armed task ids, invisibly.
}
\description{
The rework rule: `reconcile` finishes, so `qa_check` -- which already
holds a terminal `false` -- goes back to `open` and is run again. Without
this a loop is drawn but never travelled.
}
