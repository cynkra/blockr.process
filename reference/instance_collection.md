# The element list an instance was opened with

The element list an instance was opened with

## Usage

``` r
instance_collection(store = ".runs", instance = "instance")
```

## Arguments

- store:

  Store directory.

- instance:

  Instance id to filter by, or \`NULL\` for all instances.

## Value

A data frame (first column: element ids), or \`NULL\`.
